module.exports = {
    i18n: {
      locales: ['en', 'ko'],
      defaultLocale: 'en',
      localeDetection: false,
    },
    // localePath: typeof window === 'undefined' ? require('path').resolve('./src/locales') : '/locales',
  };